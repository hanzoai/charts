apiVersion: v1
kind: Service
metadata:
  name: {{ include "hanzo-iam.fullname" . }}
  labels:
    {{- include "hanzo-iam.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hanzo-iam.selectorLabels" . | nindent 4 }}
